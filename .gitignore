# Secrets — NEVER commit. App private keys, tokens, env.
*.pem
*.key
.env
.env.local
.env.*.local
config.local.*
*-credentials.*

# Go build artifacts
/gatekeeper
/dist/
/build/
*.test
*.out

# Private agent instructions / session state / crew config — NEVER commit to a released repo.
CLAUDE.md
.claude/
.crew/

# Editor / OS
.DS_Store
*.swp
*.log

# Test caches
.pytest_cache/
